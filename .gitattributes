# This project is a small prototype; Git LFS was tracking images/PDF here,
# but Vercel's default build does not fetch LFS objects, so binaries were
# being deployed as tiny pointer files instead of real images. Tracked as
# regular Git blobs instead.
